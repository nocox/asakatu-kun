spring:
  datasource:
    url:  jdbc:mysql://127.0.0.1:3306/asakatu
    username: asakatu
    password: himitu
    driverClassName: com.mysql.cj.jdbc.Driver
    initialization-mode: always
    schema: classpath:schema-product.sql

  jpa:
    hibernate:
      ddl-auto: none

  servlet:
    multipart:
      enabled: true
      file-size-threshold: 2KB
      max-file-size: 200MB
      max-request-size: 215MB